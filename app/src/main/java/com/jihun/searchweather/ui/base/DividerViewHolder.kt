package com.jihun.searchweather.ui.base

import com.jihun.searchweather.databinding.ViewDividerBinding

class DividerViewHolder(private val binding: ViewDividerBinding): BaseViewHolder(binding.root) {
    override fun onBind(data: Any?) {}
}